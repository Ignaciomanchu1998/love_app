import 'package:flutter/material.dart';

class TextCustomShared extends StatelessWidget {
  const TextCustomShared({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('TextCustomShared');
  }
}
